<%- include('partials/header') %>

  <div class="max-w-4xl mx-auto px-8 py-2  "><p class="text-xl text-center font-bold text-gray-800 mt-4"><%= homestartingcontent %></p>
    <% for(var i=0; i<posts.length; i++) { %>
      <p class="text-xl font-semibold text-gray-800 mt-5"> <%= posts[i].postTitle %></p>
      <p class="text-sm text-gray-600 mt-3 truncate overflow-hidden"> <%= posts[i].postBody %></p><a class="text-blue-300" href=<%= "/posts/"+posts[i].postTitle %> >Read More</a>
      <% } %>
    </div>
 
    
  <%- include('partials/footer') %>
